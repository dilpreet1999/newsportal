<form method="post" action="{{ path('channel_refference_delete', {'id': channel_refference.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ channel_refference.id) }}">
    <button class="btn">Delete</button>
</form>
