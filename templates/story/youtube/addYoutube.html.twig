{% extends "admin/base.html.twig" %}
{% block title %}Add Youtube Url{% endblock %}
{% block body %}
    <h1>Add Youtube Video</h1>
    <b>Story:</b><h4>{{ story.title }}</h4>
    <strong>Category</strong><h5>{{ story.category.name }}</h5>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit"/>
    {{ form_end(form) }}
    {% endblock %}