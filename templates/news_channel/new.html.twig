{% extends 'admin/base.html.twig' %}

{% block title %}New NewsChannel{% endblock %}

{% block body %}
    <h1>Create new NewsChannel</h1>

    {{ include('news_channel/_form.html.twig') }}

    <a href="{{ path('news_channel_index') }}">back to list</a>
{% endblock %}
{% block javascript %}
    <script>
   $('#news_channel_upload').on('change', function () {
            var filename = $(this).val();
            $(this).next('.custom-file-label').html(filename);
        });
        </script>
{% endblock %}