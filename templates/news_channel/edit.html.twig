{% extends 'admin/base.html.twig' %}

{% block title %}Edit NewsChannel{% endblock %}

{% block body %}
    <h1>Edit NewsChannel</h1>

    {{ include('news_channel/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('news_channel_index') }}">back to list</a>

    {{ include('news_channel/_delete_form.html.twig') }}
{% endblock %}
{% block javascript %}
    <script>
   $('#news_channel_upload').on('change', function () {
            var filename = $(this).val();
            $(this).next('.custom-file-label').html(filename);
        });
        </script>
{% endblock %}